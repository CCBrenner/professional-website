
@page "/"
@page "/{SectionId:int}"

@inject UIService UI
@inject NavigationManager NavigationManager

<SelectDemo SectionId=@SectionId />

@code {
    [Parameter]
    public int SectionId { get; set; }  // needed to pass value to component
}
