@implements IDisposable
@inject UIService UIService

<div class="container-one">
    <div class="container-two">
        <div class="nav-button @UIService.PanelMgmt.Panels[2].PanelButtonStatus" @onclick="(()=>UIService.TogglePanel(2))">
            <span class="button-icon fa-solid fa-diagram-project" aria-hidden="true"></span>
            <span class="button-label">Projects</span>
        </div>
        <div class="nav-button @UIService.PanelMgmt.Panels[3].PanelButtonStatus" @onclick="(()=>UIService.TogglePanel(3))">
            <span class="button-icon fa-solid fa-flask" aria-hidden="true"></span>
            <span class="button-label">Know-How</span>
        </div>
        <div class="nav-button @UIService.PanelMgmt.Panels[4].PanelButtonStatus" @onclick="(()=>UIService.TogglePanel(4))">
            <span class="button-icon fa-solid fa-book" aria-hidden="true"></span>
            <span class="button-label">Collyn</span>
        </div>
        <div class="nav-button @UIService.PanelMgmt.Panels[5].PanelButtonStatus" @onclick="(()=>UIService.TogglePanel(5))">
            <span class="button-icon fa-solid fa-warehouse" aria-hidden="true"></span>
            <span class="button-label">Invent</span>
        </div>
        <div class="nav-button @UIService.PanelMgmt.Panels[6].PanelButtonStatus" @onclick="(()=>UIService.TogglePanel(6))">
            <span class="button-icon fa-solid fa-newspaper" aria-hidden="true"></span>
            <span class="button-label">Articles</span>
        </div>
    </div>
</div>

@code
{
    protected override void OnInitialized() =>
        UIService.PanelMgmt.OnPanelMgmtUpdated += UpdateThisComponent;
    void IDisposable.Dispose() =>
        UIService.PanelMgmt.OnPanelMgmtUpdated += UpdateThisComponent;
    private void UpdateThisComponent(string meaningless) =>
        StateHasChanged();
}
